'use client';
import React from 'react';

const ThoughtBubble = ({ text }) => {
    return (
        <div className="thought-bubble">
            <p>{text}</p>
        </div>
    );
};

export default ThoughtBubble;
